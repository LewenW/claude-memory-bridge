---
name: import
description: Import memories from another project
---

Help the user import memories from another project into the current one:

1. Use `search_memories` with the source project to show available memories
2. Let the user pick which memories to import
3. Use `sync_memory` to copy selected memories to the current project
4. Alternatively, suggest promoting to a shared namespace if the memories are broadly useful
