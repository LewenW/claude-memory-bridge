---
name: search
description: Search memories across all projects and shared namespaces
argument: query — keywords to search for
---

Use the `search_memories` tool to search across all projects and shared namespaces for the user's query. Display results clearly with source project/namespace, relevance score, and matching context. If no results, suggest broadening the query or checking different scopes.
